@model Azmoonak.Database.Models.Group

<div dir="rtl">
    <h6 class="d-inline-block">حذف گروه @Model.GroupName ؟</h6>
    <form asp-action="DeleteEntryGroup" class="d-inline-block">
        <input asp-for="Id" readonly hidden />
        <div>
            <input type="submit" value="حذف" class="btn btn-danger" />
            <a id='@("btn-cancel-"+Model.Id)' class="btn btn-outline-danger">لغو</a>
        </div>
    </form>
</div>

